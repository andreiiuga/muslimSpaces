# Server-only — never prefix with NEXT_PUBLIC_. All backend calls happen in
# Server Components / Route Handlers, never from the browser, so the backend
# URL doesn't need to be exposed to client JS.
API_BASE_URL=http://localhost:3001
